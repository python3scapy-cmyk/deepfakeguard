opencv-python<5,>=4.8
numpy
librosa
requests

# Visual deepfake classifier (Stage 2) -- real pretrained model,
# auto-downloaded from Hugging Face on first run (prithivMLmods/deepfake-detector-model-v1)
torch
torchvision
transformers
pillow

# Identity matching (ArcFace) -- used by security/enroll.py, security/verify.py,
# and security/identity.py (imported directly by main.py as of Week 3 fix #4)
insightface
onnxruntime

# Real microphone capture for main.py's audio pipeline (fix #5) -- replaces
# the mock np.random.rand() audio previously fed into lip-sync/AASIST.
sounddevice

# Flask backend (backend/app.py) -- previously undeclared here even though
# the backend depends on all three.
flask
flask-socketio
flask-cors

# Spoken-phrase liveness challenge (audio_module/speech_challenge.py).
# WITHOUT this the voice challenge silently degrades to energy-only mode:
# it can tell that someone spoke, but NOT whether they said the phrase.
faster-whisper

# Audio spoof training script (audio_module/train_xgb.py)
xgboost
kagglehub
scikit-learn
# Test suite (tests/test_fusion_parity.py)
pytest