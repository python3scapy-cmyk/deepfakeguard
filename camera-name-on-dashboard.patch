--- orig/deepfakeguard-main/backend/app.py	2026-07-14 08:46:32.000000000 +0000
+++ deepfakeguard-main/backend/app.py	2026-07-14 11:25:03.022024526 +0000
@@ -104,6 +104,11 @@
         # Rolling summary for the admin's client list
         self.summary = {"trust_score": None, "band": None, "verdict": None,
                         "session_final": False}
+        # Camera identity, as classified client-side in participant.html
+        # (browser is the only place that can read the MediaStreamTrack
+        # label). Display-only — never affects the trust score.
+        self.camera_name = None
+        self.camera_class = None
 
     def touch(self):
         self.last_active = time.time()
@@ -1135,6 +1140,10 @@
             "injection_risk": score_to_tier(flat_scores["injection_risk_score"])  if flat_scores.get("injection_risk_score")  is not None else None
         },
         "signal_missing": signal_missing,
+        "camera": {
+            "name":  state.camera_name,
+            "class": state.camera_class,   # "physical" | "suspect" | "virtual" | None
+        },
         "modules_reporting": [k for k, v in state.latest_scores.items() if v is not None],
         "session_log": state.session_log[-30:],
         # Admin view: who else is verifying in this room right now.
@@ -1281,6 +1290,15 @@
         yaw_ratio=yaw_ratio if isinstance(yaw_ratio, (int, float)) else None)
     if fusion:
         state = room.client(sid)          # per-client state, not a global one
+        # Camera label is display-only and sent on every frame, but only
+        # bother updating state (and logging) when it actually changes —
+        # avoids a log line 10x/sec.
+        cam_name = data.get('camera_name')
+        cam_class = data.get('camera_class')
+        if cam_name and cam_name != state.camera_name:
+            state.camera_name = cam_name
+            state.camera_class = cam_class
+            print(f"[ROOM {room.code}] {sid} camera: {cam_name} ({cam_class})")
         ingest_fusion(fusion, state)
         state.summary = {
             "trust_score": fusion.get("trust_score"),
--- orig/deepfakeguard-main/frontend/participant.html	2026-07-14 08:46:32.000000000 +0000
+++ deepfakeguard-main/frontend/participant.html	2026-07-14 11:24:40.003377887 +0000
@@ -1169,6 +1169,8 @@
         code: ROOM,
         ear: (typeof window.__DFG_EAR === 'number') ? window.__DFG_EAR : null,
         yaw_ratio: (typeof window.__DFG_YAW === 'number') ? window.__DFG_YAW : null,
+        camera_name: window.__DFG_CAMERA_NAME || null,
+        camera_class: window.__DFG_CAMERA_CLASS || null,
         jpg: analysisCanvas.toDataURL('image/jpeg', 0.7)
       });
     }, 1000 / ANALYSIS_FPS);
@@ -1501,6 +1503,12 @@
     if (textEl) { textEl.textContent = info.name; textEl.className = 'vf-camera-text ' + info.cls; }
     if (tagEl) { tagEl.textContent = info.tag; tagEl.className = 'vf-camera-tag ' + (info.cls === 'unknown' ? '' : info.cls); }
     console.log('[DFG] active camera:', info.name, '->', info.cls);
+    // Stash the classification so the analysis_frame stream can forward it
+    // to the backend, which in turn forwards it to the verifier dashboard
+    // (index.html). Without this, the camera name never leaves this
+    // browser — it's display-only, computed from the track label above.
+    window.__DFG_CAMERA_NAME = info.name || null;
+    window.__DFG_CAMERA_CLASS = info.cls || null;
   }
 
   function setStatus(text, live) {
--- orig/deepfakeguard-main/frontend/index.html	2026-07-14 08:46:32.000000000 +0000
+++ deepfakeguard-main/frontend/index.html	2026-07-14 11:25:34.115125828 +0000
@@ -74,6 +74,14 @@
         @keyframes badgePulse{0%,100%{opacity:1;}50%{opacity:.65;}}
         .video-meta{position:absolute;bottom:16px;right:16px;display:flex;gap:6px;}
         .meta-pill{background:rgba(0,0,0,.55);backdrop-filter:blur(8px);color:rgba(255,255,255,.85);padding:4px 10px;border-radius:6px;font-size:11px;font-weight:600;}
+        .camera-pill{background:rgba(0,0,0,.55);backdrop-filter:blur(8px);padding:4px 10px;border-radius:6px;font-size:11px;font-weight:600;display:flex;align-items:center;gap:6px;color:rgba(255,255,255,.85);}
+        .camera-pill .cam-dot{width:6px;height:6px;border-radius:50%;background:#7c88ab;flex-shrink:0;}
+        .camera-pill.physical .cam-dot{background:#22c55e;}
+        .camera-pill.suspect  .cam-dot{background:#f59e0b;}
+        .camera-pill.virtual  .cam-dot{background:#f87171;}
+        .camera-pill.physical{color:#22c55e;}
+        .camera-pill.suspect {color:#f59e0b;}
+        .camera-pill.virtual {color:#f87171;}
         .video-timer{position:absolute;bottom:16px;left:16px;background:rgba(0,0,0,.55);backdrop-filter:blur(8px);color:rgba(255,255,255,.75);padding:4px 10px;border-radius:6px;font-size:11px;font-weight:600;font-variant-numeric:tabular-nums;}
         .pip-window{position:absolute;bottom:56px;right:16px;width:200px;aspect-ratio:4/3;background:#060a14;border-radius:10px;border:2px solid rgba(255,255,255,.18);overflow:hidden;box-shadow:0 8px 32px rgba(0,0,0,.6);cursor:move;z-index:50;transition:border-color .2s,box-shadow .2s;user-select:none;touch-action:none;}
         .pip-window:hover{border-color:rgba(107,138,255,.6);}
@@ -585,6 +593,10 @@
             </div>
             <div class="video-timer" id="sessionTimer">00:00</div>
             <div class="video-meta">
+                <span class="camera-pill" id="cameraPill" title="Camera reported by the participant's browser">
+                    <span class="cam-dot" aria-hidden="true"></span>
+                    <span id="cameraPillText">Camera: detecting&hellip;</span>
+                </span>
                 <span class="meta-pill">720p</span>
                 <span class="meta-pill">30 fps</span>
             </div>
@@ -1312,6 +1324,25 @@
     document.getElementById('srStatus').textContent =
         'Trust score ' + trust + ' out of 100, ' + s.text + '. ' + (data.reason || '');
 
+    // ── Camera name pill (reported client-side by participant.html) ──
+    const camPill = document.getElementById('cameraPill');
+    const camText = document.getElementById('cameraPillText');
+    if (camPill && camText) {
+        const cam = data.camera || {};
+        const camLabel = {
+            physical: 'Physical camera',
+            suspect:  'Possible virtual camera',
+            virtual:  'Virtual camera'
+        };
+        if (cam.name) {
+            camText.textContent = cam.name + (camLabel[cam.class] ? ' (' + camLabel[cam.class] + ')' : '');
+            camPill.className = 'camera-pill' + (cam.class ? ' ' + cam.class : '');
+        } else {
+            camText.textContent = 'Camera: detecting…';
+            camPill.className = 'camera-pill';
+        }
+    }
+
     // ── Conflict Alert Banner (Week 3 Day 4) ────
     const conflictAlert = document.getElementById('conflictAlert');
     const conflictText = document.getElementById('conflictText');
